apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory
  template:
    metadata:
      labels:
        app: inventory
    spec:
      containers:
      - name: inventory-container
        image: atulkamble.azurecr.io/aks-python-app  # Update this to your DockerHub or ACR image
        ports:
        - containerPort: 5000
